[ -f ~/bash-sensible/sensible.bash ] && . ~/bash-sensible/sensible.bash
[ -f /usr/local/etc/bash_completion ] && . /usr/local/etc/bash_completion 
export CLICOLOR=
export EDITOR=$(which vim)
alias gs='git status '
alias ga='git add '
alias gb='git branch '
alias gc='git commit'
alias gd='git diff'
alias go='git checkout '
alias gk='gitk --all&'
alias gx='gitx --all'

alias got='git '
alias get='git '
